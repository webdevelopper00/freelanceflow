# Dev: use file:./dev.db for SQLite. Prod (Railway): use PostgreSQL URL and run db:generate:prod before build.
DATABASE_URL=
JWT_SECRET=
JWT_REFRESH_SECRET=
PORT=5000
NODE_ENV=production
FRONTEND_URL=https://your-app.vercel.app
